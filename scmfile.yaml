credential:
  usernamePassword:
    password: secret
    scope: GLOBAL
    id: credentials
    username: user1
scm:
  git:
    userRemoteConfigs:
    - url: https://github.com/inspirit941/todo-with-cicd.git
      credentialsId: credentials
    branches:
    - name: master
